---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visiting / shows a landing page with product explanation"
    - "Landing page has a hero section with headline, subhead, and signup CTA"
    - "Landing page explains how the product works in 3 clear steps"
    - "Signup CTA button links to /signup"
    - "Page looks polished with dark-mode-first design (dark bg, white text, proper spacing)"
    - "Page is responsive — readable on mobile and desktop"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Landing page content — hero, how it works, features, CTA"
      min_lines: 80
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Marketing layout with header nav and footer"
      min_lines: 30
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "/signup"
      via: "CTA button href"
      pattern: "href.*signup"
    - from: "src/app/(marketing)/layout.tsx"
      to: "/login"
      via: "Header nav link"
      pattern: "href.*login"
---

<objective>
Build the public landing page at / that explains Unlockt, shows how it works, and drives signups. Includes marketing layout with header navigation and footer.

Purpose: PAGE-01 requires a landing page that explains the product, shows how it works, and has a working signup CTA. This is the first thing potential creators see.
Output: A polished landing page with marketing layout, hero section, how-it-works steps, feature highlights, and CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marketing layout and landing page</name>
  <files>
    src/app/(marketing)/layout.tsx
    src/app/(marketing)/page.tsx
  </files>
  <action>
    **Marketing layout (`src/app/(marketing)/layout.tsx`):**
    - Server component (no 'use client' needed)
    - Structure: header (sticky top) + main content + footer
    - Header:
      - Container: max-w-6xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between
      - Left: "unlockt" logo text (font-bold text-lg tracking-tight text-white)
      - Right: nav links — "Login" (text-muted-foreground hover:text-white transition) linking to /login, "Get Started" button (use shadcn Button, variant default/primary — white bg black text) linking to /signup
      - Sticky header: `sticky top-0 z-50 bg-background/80 backdrop-blur-sm border-b border-border`
    - Footer:
      - Container: max-w-6xl mx-auto px-4 sm:px-6 py-8
      - Simple footer: "© 2026 Unlockt. All rights reserved." in text-muted-foreground text-sm
      - Centered text

    **Landing page (`src/app/(marketing)/page.tsx`):**
    - Server component (no client interactivity needed beyond links)
    - Container: max-w-6xl mx-auto px-4 sm:px-6

    **Section 1: Hero**
    - Padding: py-20 sm:py-32 text-center
    - Badge/pill above headline: "The simplest way to sell digital files" — small rounded-full bg-secondary text-sm px-4 py-1.5 text-muted-foreground inline-block mb-6
    - Headline: "Upload. Price. Share. Get paid." — text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-white, max-w-3xl mx-auto
    - Subheadline: "Lock any digital file behind a payment link. Share it anywhere. Buyers pay and download instantly. No marketplace, no subscriptions — just sales." — text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto mt-6
    - CTA buttons (flex gap-4 justify-center mt-10):
      - Primary: "Start selling" -> /signup (shadcn Button, size lg)
      - Secondary: "See how it works" -> scroll anchor #how-it-works (shadcn Button, variant outline, size lg — border-border text-white hover:bg-secondary)

    **Section 2: How it works (id="how-it-works")**
    - Padding: py-20 sm:py-24
    - Section heading: "How it works" — text-3xl font-bold tracking-tight text-center mb-4
    - Subheading: "Three steps. Under two minutes." — text-muted-foreground text-center mb-12 sm:mb-16
    - Three-column grid (grid grid-cols-1 md:grid-cols-3 gap-8):
      - Step 1: Upload — icon (Upload from lucide-react), "Upload your file", "Upload any digital file — PDFs, images, videos, ZIPs — up to 500MB."
      - Step 2: Set your price — icon (DollarSign or Coins from lucide), "Set your price", "Choose your currency (CHF, EUR, USD, GBP) and set your price. We handle the rest."
      - Step 3: Share & earn — icon (Share2 or Link from lucide), "Share & earn", "Get a unique payment link. Share it anywhere — social media, email, DMs. Buyers pay and download instantly."
    - Each step: card-like styling with bg-card border border-border rounded-xl p-6 sm:p-8 text-center (or left-aligned). Step number in text-accent font-bold mb-4. Icon in text-accent w-10 h-10 mx-auto mb-4.

    **Section 3: Features/Benefits**
    - Padding: py-20 sm:py-24 border-t border-border
    - Section heading: "Built for creators" — text-3xl font-bold tracking-tight text-center mb-12
    - Two-column grid (grid grid-cols-1 sm:grid-cols-2 gap-6):
      - Feature cards (bg-card border border-border rounded-xl p-6):
        1. "Instant downloads" — "Buyers pay and download immediately. No waiting, no friction."
        2. "No account needed for buyers" — "Your customers just pay and download. Zero signup required."
        3. "Multi-currency" — "Sell in CHF, EUR, USD, or GBP. You choose per link."
        4. "Secure delivery" — "Files are never publicly accessible. Time-limited signed URLs only."
      - Each card: title in font-semibold text-white mb-2, description in text-muted-foreground text-sm

    **Section 4: Final CTA**
    - Padding: py-20 sm:py-32 text-center border-t border-border
    - Heading: "Ready to start selling?" — text-3xl sm:text-4xl font-bold tracking-tight
    - Subheading: "Create your first payment link in under 2 minutes." — text-muted-foreground mt-4
    - CTA button: "Create your account" -> /signup (shadcn Button, size lg, mt-8)
  </action>
  <verify>
    - `npx tsc --noEmit` — zero errors
    - Visit http://localhost:3000 — landing page renders with all 4 sections
    - "Start selling" button links to /signup
    - "Login" in header links to /login
    - Responsive: resize browser to mobile width — content stacks vertically, text remains readable
    - Dark theme is consistently applied (no white/light sections)
  </verify>
  <done>
    Landing page renders at / with hero, how-it-works (3 steps), features (4 cards), and final CTA. Marketing layout has sticky header with nav (Login + Get Started) and footer. All CTAs link to /signup. Responsive on mobile and desktop. Dark-mode-first design consistent throughout.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete landing page at / with:
    - Sticky header (logo + Login + Get Started)
    - Hero section with headline, subheadline, and CTA buttons
    - "How it works" 3-step section with icons
    - "Built for creators" 4-feature grid
    - Final CTA section
    - Footer
    All using the dark-mode-first design system (dark bg, white text, purple accent, card borders).
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000
    2. Verify hero section: headline reads "Upload. Price. Share. Get paid.", CTA button links to /signup
    3. Scroll to "How it works" — 3 steps displayed clearly
    4. Scroll to features — 4 feature cards visible
    5. Scroll to final CTA — button links to /signup
    6. Click "Login" in header — navigates to /login
    7. Click "Get Started" in header — navigates to /signup
    8. Resize to mobile width — verify content stacks and remains readable
    9. Check overall aesthetic: dark background, proper spacing, no visual glitches
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/content issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. http://localhost:3000 renders the full landing page
2. All CTA buttons link to /signup
3. Header Login links to /login
4. Page is responsive (mobile and desktop)
5. Dark mode design is consistent — no white/light flashes
6. All text is readable (sufficient contrast)
7. Icons render from lucide-react
8. Smooth scroll to #how-it-works works (or at minimum the anchor exists)
</verification>

<success_criteria>
- Landing page at / explains Unlockt: what it is, how it works, and why creators should use it
- Hero with headline, subheadline, and signup CTA
- How-it-works section with 3 clear steps
- Features section highlighting key benefits
- Final CTA section driving signups
- Marketing layout with sticky header (Login + Get Started) and footer
- All links functional (/signup, /login)
- Responsive design — works on mobile and desktop
- Dark-mode-first aesthetic throughout (PAGE-01 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
